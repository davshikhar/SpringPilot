name: Deploying the application to EC2
on:
  push:
    branches: [Production]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{secrets.DOCKER_USERNAME}}
          password: ${{secrets.DOCKER_PASSWORD}}

      - name: Build and push the image to docker hub
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: shikhar6082/olib:${{github.sha}}

#      - name: Deploy to the VM on AWS
#        run: |
#          echo "${{secrets.SSH_PRIVATE_KEY}}" > ~/ssh_key
#          chmod 600 /home/runner/ssh_key
#          ssh -o StrictHostKeyChecking=no -i ~/ssh_key ubuntu@13.232.42.126 "docker compose down && docker pull shikhar6082/OLib:${{github.sha}} && docker compose up -d --remove-orphans && docker run -p 8080:8080

